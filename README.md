# Worktree and clone scripts
These are wrappers around git to work with worktrees more efficiently.  The clone script when given a git repository url will clone into `project_name/[master/main/trunk]/` and the worktree script provides a way to create a branch and respective worktrees at `project_name/[feat/fix/docs]/[details]`.
